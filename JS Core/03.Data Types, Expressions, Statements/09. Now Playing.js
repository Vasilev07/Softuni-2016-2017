function solve([track, artist, duration]) {
    let artistName = artist;
    let trackName = track;
    let durationOfTheSong = duration;
 console.log("Now Playing: " + artistName + " - " + trackName + " " + "[" + durationOfTheSong + "]")
}
solve(['Number One', 'Nelly', '4:09']);